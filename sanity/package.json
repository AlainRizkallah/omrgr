{
  "name": "showcase-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Sanity Studio for showcase site",
  "scripts": {
    "dev": "sanity dev",
    "sync-env": "node scripts/sync-env.js",
    "build": "node scripts/sync-env.js && sanity build",
    "deploy": "node scripts/sync-env.js && sanity deploy"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "sanity": "^3.99.0",
    "styled-components": "^6.3.9"
  }
}
